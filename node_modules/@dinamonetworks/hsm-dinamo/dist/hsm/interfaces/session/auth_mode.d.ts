import AuthUsernamePassword from "./auth_username_password";
/**
 * @internal
 * Modos de autenticação suportados
 * @property {AuthUsernamePassword} authUsernamePassword - Autenticação por usuário e senha
 * @property {string} authToken - Autenticação por token
 */
export default interface AuthMode {
    authUsernamePassword?: AuthUsernamePassword;
    authToken?: string;
}
