/**
 * @internal
 * Cabeçalho do protocolo AAP
 * @property {number} id Identificador do protocolo
 * @property {number} len Tamanho do payload
 */
export default interface AapHdr {
    id: number;
    len: number;
}
