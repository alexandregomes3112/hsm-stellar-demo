/**
 * Valida se o arquivo informado existe.
 * @param {string} filePath Caminho do arquivo.
 * @param {(reason?: any) => void} reject Função de rejeição da promise.
 * @param {string} [message] Mensagem de erro.
 * @default message = `File "${filePath}" not found.`
 */
export declare function fileExistsValidator(filePath: string, reject: (reason?: unknown) => void, message?: string): void;
