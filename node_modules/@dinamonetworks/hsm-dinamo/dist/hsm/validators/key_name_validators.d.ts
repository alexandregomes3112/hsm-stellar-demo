/**
 * Verifica se o nome da chave é válido.
 * @param {string} keyName Nome da chave.
 * @param {(reason?: unknown, attribute?: string) => void} reject Função de rejeição da promise.
 * @param {string} message Mensagem de erro que será retornada caso o nome da chave seja invalida.
 * @default message = 'Invalid key name: (${keyName}). Key name must be less than or equal to ${constants.MAX_OBJ_FQN_LEN} characters. Please, check the documentation.'
 */
export declare function keyNameValidator(keyName: string, reject: (reason?: unknown) => void, message?: string): void;
