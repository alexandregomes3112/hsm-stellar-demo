import * as interfaces from "../interfaces/validators";
/**
 * @internal
 * Verifica se o dado é um base64 válido.
 */
export declare function base64Validate(options: interfaces.Base64Options): void;
