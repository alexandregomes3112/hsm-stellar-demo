import tls from "tls";
import HsmBaseModule from "./hsm_base_module";
import * as interfaces from "../interfaces/svault";
export default class Token extends HsmBaseModule implements interfaces.Token {
    constructor(conn: tls.TLSSocket);
    create(flags: number, cks: number, from: number, to: number, secret: string, keyName: string): Promise<Buffer>;
    getSecret(maskedData: Buffer, keyName: string, mask?: string | null): Promise<Buffer>;
    getToken(maskedData: Buffer, keyName: string, mask?: string | null): Promise<Buffer>;
}
