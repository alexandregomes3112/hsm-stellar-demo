import tls from "tls";
import * as interfaces from "../interfaces/hash";
import * as enums from "../enums";
import HsmBaseModule from "./hsm_base_module";
export default class Hash extends HsmBaseModule implements interfaces.Hash {
    constructor(conn: tls.TLSSocket);
    /**
     * Inicializa um contexto de hash para ser usado com um fluxo de dados. Esse contexto é usado para chamadas subsequentes entre outras.
     * @param {enums.HASH_ALGORITHMS} algorithm - Algoritmo de hash
     * @param {string | null} keyName - Nome da chave de hash
     * @returns {Promise<Buffer>} - Retorna um buffer com o contexto de hash inicializado
     */
    create(algorithm: enums.HASH_ALGORITHMS, keyName: string | null): Promise<Buffer>;
    hashData(hash: Buffer, data: Buffer): Promise<boolean>;
    get(hash: Buffer): Promise<Buffer>;
}
