import tls from "tls";
import HsmBaseModule from "./hsm_base_module";
import * as interfaces from "../interfaces";
import * as constants from "../constants";
import * as enums from "../enums";
export default class User extends HsmBaseModule implements interfaces.user.User {
    private _user;
    constructor(conn: tls.TLSSocket, user: string | null);
    create(userId: string, password: string, permissions?: Array<enums.USER_PERMISSIONS>): Promise<boolean>;
    remove(userId: string): Promise<boolean>;
    block(userId: string): Promise<boolean>;
    unblock(userId: string): Promise<boolean>;
    blockUnblock(userId: string, action: typeof constants.MSBU_OP_BLOCK | typeof constants.MSBU_OP_UNBLOCK): Promise<boolean>;
    changePassword(newPassword: string): Promise<boolean>;
    listTokens(): Promise<Array<string>>;
    revokeToken(token: string): Promise<boolean>;
    generateToken(expiration?: Date): Promise<string>;
    private managerToken;
    private parseToken;
    private permissionsConcat;
}
