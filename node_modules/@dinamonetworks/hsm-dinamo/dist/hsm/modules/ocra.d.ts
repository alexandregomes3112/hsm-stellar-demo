import tls from "tls";
import HsmBaseModule from "./hsm_base_module";
import * as interfaces from "../interfaces";
export default class Ocra extends HsmBaseModule implements interfaces.ocra.Ocra {
    constructor(conn: tls.TLSSocket);
    ocraGen(sk: string, q: string, suite: string, len: number, c?: bigint | null, ph?: string | null, s?: string | null, ts?: bigint | null): Promise<string>;
    private ocraLenValidate;
    private keyNameValidate;
    private sessionValidate;
    private isAsciiEncode;
}
