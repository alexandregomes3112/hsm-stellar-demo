/**
 * Erros retornados pelo HSM ou pela biblioteca
 */
export default class HsmError extends Error {
    message: string;
    errorCode: number;
    constructor(message: string, errorCode?: number);
    getMessage(): string;
}
