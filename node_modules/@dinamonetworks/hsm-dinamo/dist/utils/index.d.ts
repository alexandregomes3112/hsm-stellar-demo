import Logger from "./logger";
import Aap from "./aap";
import { HASH_ALGORITHMS } from "../hsm/enums";
/**
 * @internal
 * Check if hash algorithm is not a HMAC algorithm
 * @param {HASH_ALGORITHMS} hashAlgorithm - Hash algorithm
 * @returns {boolean} True if hash algorithm is not a HMAC algorithm
 */
declare function isHashKeyNull(hashAlgorithm: HASH_ALGORITHMS): boolean;
declare function isHex(str: string): boolean;
export { Logger, Aap, isHashKeyNull, isHex, };
